# คู่มือการตั้งค่า (Setup Guide)

## ขั้นตอนการตั้งค่าแบบละเอียด

### 1. ติดตั้ง Dependencies

```bash
npm install
```

### 2. สร้างบัญชี Supabase

1. ไปที่ [https://supabase.com](https://supabase.com)
2. คลิก **Start your project**
3. สร้างบัญชีด้วย GitHub, Google หรือ Email
4. คลิก **New Project**
5. ตั้งค่า:
   - **Name**: ชื่อโปรเจกต์ (เช่น real-estate-listing)
   - **Database Password**: ตั้งรหัสผ่านที่แข็งแรง (บันทึกไว้!)
   - **Region**: เลือกที่ใกล้ที่สุด (Southeast Asia - Singapore)
6. คลิก **Create new project**
7. รอให้สร้างเสร็จ (ประมาณ 2-3 นาที)

### 3. ตั้งค่า Database

1. ใน Supabase Dashboard ไปที่ **SQL Editor** (เมนูซ้าย)
2. คลิก **New query**
3. เปิดไฟล์ `supabase/schema.sql` ในโปรเจกต์ของคุณ
4. คัดลอกโค้ดทั้งหมด
5. วางใน SQL Editor
6. คลิก **Run** (หรือกด Ctrl+Enter)
7. ตรวจสอบว่าไม่มี Error

### 3.1. ตั้งค่าระบบสมาชิก (Auth)

1. ใน SQL Editor สร้าง query ใหม่
2. เปิดไฟล์ `supabase/auth-schema.sql` ในโปรเจกต์ของคุณ
3. คัดลอกโค้ดทั้งหมด
4. วางใน SQL Editor
5. คลิก **Run**
6. ตรวจสอบว่า Table `profiles` ถูกสร้างแล้ว

### 4. สร้าง Storage Bucket

1. ใน Supabase Dashboard ไปที่ **Storage** (เมนูซ้าย)
2. คลิก **New bucket**
3. ตั้งค่า:
   - **Name**: `listings`
   - **Public bucket**: ✅ เปิด (สำคัญ!)
4. คลิก **Create bucket**

### 5. ตั้งค่า Storage Policies

1. ไปที่ **Storage** → **Policies**
2. คลิกที่ bucket `listings`
3. คลิก **New Policy**
4. เลือก **For full customization**
5. ตั้งชื่อ: `Allow public read access`
6. ใช้ SQL นี้:

```sql
CREATE POLICY "Allow public read access"
ON storage.objects FOR SELECT
USING (bucket_id = 'listings');
```

7. คลิก **Review** แล้ว **Save policy**

### 6. ตั้งค่า Environment Variables

1. ใน Supabase Dashboard ไปที่ **Settings** → **API**
2. คัดลอก:
   - **Project URL** (อยู่ด้านบน)
   - **anon public** key (ในส่วน Project API keys)

3. สร้างไฟล์ `.env.local` ในโฟลเดอร์โปรเจกต์:

```env
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**⚠️ ระวัง**: 
- อย่า commit ไฟล์ `.env.local` ลง Git
- ไฟล์นี้มีข้อมูลสำคัญ อย่าแชร์ให้ใคร

### 7. ทดสอบการทำงาน

```bash
npm run dev
```

เปิดเบราว์เซอร์ไปที่ [http://localhost:3000](http://localhost:3000)

### 8. ทดสอบการลงประกาศ

1. คลิก **ลงประกาศฟรี**
2. กรอกข้อมูล
3. อัปโหลดรูปภาพ
4. คลิก **ลงประกาศ**
5. ตรวจสอบว่าประกาศแสดงในหน้าหลัก

## 🔍 ตรวจสอบปัญหา

### รูปภาพไม่แสดง

**ปัญหา**: รูปภาพอัปโหลดแล้วแต่ไม่แสดง

**แก้ไข**:
1. ตรวจสอบว่า Storage bucket เป็น Public
2. ตรวจสอบว่า Policy อนุญาตให้อ่านได้
3. ตรวจสอบ URL ของรูปภาพใน Supabase Storage

### Database Error

**ปัญหา**: Error เมื่อดึงข้อมูล

**แก้ไข**:
1. ตรวจสอบว่าได้รัน SQL schema แล้ว
2. ตรวจสอบว่า Table `listings` มีอยู่
3. ตรวจสอบ Environment Variables

### Build Error

**ปัญหา**: Build ไม่ผ่าน

**แก้ไข**:
```bash
# ลบ cache
rm -rf .next node_modules

# ติดตั้งใหม่
npm install

# Build ใหม่
npm run build
```

## 📝 Checklist

- [ ] ติดตั้ง dependencies แล้ว
- [ ] สร้าง Supabase project แล้ว
- [ ] รัน SQL schema แล้ว
- [ ] สร้าง Storage bucket แล้ว
- [ ] ตั้งค่า Storage policies แล้ว
- [ ] ตั้งค่า Environment Variables แล้ว
- [ ] ทดสอบรันเว็บไซต์ได้แล้ว
- [ ] ทดสอบลงประกาศได้แล้ว

## 🚀 พร้อม Deploy!

เมื่อทุกอย่างทำงานได้แล้ว ดูคำแนะนำการ Deploy ใน `README.md`

