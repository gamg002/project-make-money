# 🚀 คู่มือตั้งค่าให้ Frontend และ Backend ทำงานร่วมกัน

## ขั้นตอนที่ 1: สร้าง Supabase Project (5 นาที)

### 1.1 สร้างบัญชี Supabase
1. ไปที่ [https://supabase.com](https://supabase.com)
2. คลิก **Start your project**
3. สร้างบัญชีด้วย GitHub, Google หรือ Email

### 1.2 สร้างโปรเจกต์ใหม่
1. คลิก **New Project**
2. ตั้งค่า:
   - **Name**: `real-estate-listing`
   - **Database Password**: ตั้งรหัสผ่านที่แข็งแรง (บันทึกไว้!)
   - **Region**: **Southeast Asia (Singapore)**
3. คลิก **Create new project**
4. รอให้สร้างเสร็จ (ประมาณ 2-3 นาที)

### 1.3 หา API Keys
1. ใน Supabase Dashboard ไปที่ **Settings** (ไอคอนฟันเฟือง)
2. คลิก **API** ในเมนูซ้าย
3. คัดลอก:
   - **Project URL** (อยู่ด้านบน) - คลิก Copy
   - **anon public** key (ในส่วน Project API keys) - คลิก Copy

---

## ขั้นตอนที่ 2: ตั้งค่า Environment Variables

### วิธีที่ 1: ใช้ Script (แนะนำ)

```bash
./setup-supabase.sh
```

Script จะถาม Supabase URL และ Key แล้วสร้างไฟล์ `.env.local` ให้อัตโนมัติ

### วิธีที่ 2: สร้างเอง

1. สร้างไฟล์ `.env.local` ในโฟลเดอร์โปรเจกต์
2. ใส่ข้อมูล:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
```

**⚠️ ระวัง:** อย่า commit ไฟล์ `.env.local` ลง Git

---

## ขั้นตอนที่ 3: ตั้งค่า Database

### 3.1 รัน SQL Schema

1. ใน Supabase Dashboard ไปที่ **SQL Editor** (เมนูซ้าย)
2. คลิก **New query**
3. เปิดไฟล์ `supabase/schema.sql` ในโปรเจกต์ของคุณ
4. คัดลอกโค้ดทั้งหมด
5. วางใน SQL Editor
6. คลิก **Run** (หรือกด Ctrl+Enter)
7. ตรวจสอบว่าไม่มี Error

### 3.2 รัน Auth Schema

1. ใน SQL Editor สร้าง query ใหม่
2. เปิดไฟล์ `supabase/auth-schema.sql`
3. คัดลอกโค้ดทั้งหมด
4. วางใน SQL Editor
5. คลิก **Run**
6. ตรวจสอบว่า Table `profiles` ถูกสร้างแล้ว

---

## ขั้นตอนที่ 4: สร้าง Storage Bucket

### 4.1 สร้าง Bucket

1. ใน Supabase Dashboard ไปที่ **Storage** (เมนูซ้าย)
2. คลิก **New bucket**
3. ตั้งค่า:
   - **Name**: `listings`
   - **Public bucket**: ✅ **เปิด** (สำคัญมาก!)
4. คลิก **Create bucket**

### 4.2 ตั้งค่า Storage Policy

1. ไปที่ **Storage** → **Policies**
2. คลิกที่ bucket `listings`
3. คลิก **New Policy**
4. เลือก **For full customization**
5. ตั้งชื่อ: `Allow public read access`
6. ใช้ SQL นี้:

```sql
CREATE POLICY "Allow public read access"
ON storage.objects FOR SELECT
USING (bucket_id = 'listings');
```

7. คลิก **Review** แล้ว **Save policy**

---

## ขั้นตอนที่ 5: รีสตาร์ทเซิร์ฟเวอร์

```bash
# หยุดเซิร์ฟเวอร์ (กด Ctrl+C)
# แล้วรันใหม่:
npm run dev
```

---

## ✅ ตรวจสอบการทำงาน

### 1. ตรวจสอบหน้า Setup
ไปที่: **http://localhost:3000/setup**

ควรเห็น:
- ✅ NEXT_PUBLIC_SUPABASE_URL ✓ ตั้งค่าแล้ว
- ✅ NEXT_PUBLIC_SUPABASE_ANON_KEY ✓ ตั้งค่าแล้ว

### 2. ทดสอบการทำงาน
1. เปิด **http://localhost:3000**
2. ไม่ควรเห็น warning "โหมด Demo" แล้ว
3. คลิก **สมัครสมาชิก** → สร้างบัญชี
4. **ลงประกาศ** → อัปโหลดรูปภาพ
5. **ค้นหา** → ควรเห็นประกาศที่ลงไว้

---

## 🐛 แก้ไขปัญหา

### Error: "Supabase not configured"
- ตรวจสอบว่าไฟล์ `.env.local` มีอยู่
- ตรวจสอบว่า URL และ Key ถูกต้อง
- รีสตาร์ทเซิร์ฟเวอร์

### Error: "relation does not exist"
- ตรวจสอบว่าได้รัน SQL schema แล้ว
- ตรวจสอบว่า Table `listings` และ `profiles` มีอยู่

### รูปภาพไม่แสดง
- ตรวจสอบว่า Storage bucket เป็น Public
- ตรวจสอบว่า Policy ตั้งค่าแล้ว
- ตรวจสอบ URL ของรูปภาพ

### ไม่สามารถสมัครสมาชิกได้
- ตรวจสอบว่าได้รัน `auth-schema.sql` แล้ว
- ตรวจสอบว่า Table `profiles` มีอยู่
- ตรวจสอบ Console สำหรับ error messages

---

## 📞 ต้องการความช่วยเหลือ?

- ดู `QUICK_START.md` สำหรับคำแนะนำแบบเร็ว
- ดู `SETUP.md` สำหรับคำแนะนำแบบละเอียด
- ดู `AUTH_GUIDE.md` สำหรับระบบสมาชิก

---

## 🎉 เสร็จสิ้น!

ตอนนี้เว็บไซต์ของคุณพร้อมใช้งานแล้ว:
- ✅ Frontend และ Backend ทำงานร่วมกัน
- ✅ ระบบสมาชิกใช้งานได้
- ✅ ลงประกาศได้
- ✅ อัปโหลดรูปภาพได้
- ✅ ค้นหาและกรองได้

