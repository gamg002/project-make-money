# 📋 คำแนะนำการตั้งค่าต่อจากนี้

## ✅ สิ่งที่ทำเสร็จแล้ว
- ✅ ตั้งค่า .env.local ด้วย Supabase URL และ Key ของคุณแล้ว

## 🔧 ขั้นตอนต่อไป (ทำใน Supabase Dashboard)

### ขั้นตอนที่ 1: ตั้งค่า Database (5 นาที)

1. ไปที่ **Supabase Dashboard**: https://supabase.com/dashboard/project/shkvdwuryywrlrvsvngl
2. คลิก **SQL Editor** (เมนูซ้าย)
3. คลิก **New query**

#### 1.1 รัน Schema หลัก
1. เปิดไฟล์ `supabase/schema.sql` ในโปรเจกต์ของคุณ
2. คัดลอกโค้ดทั้งหมด
3. วางใน SQL Editor
4. คลิก **Run** (หรือกด Ctrl+Enter)
5. ตรวจสอบว่าไม่มี Error

#### 1.2 รัน Auth Schema
1. สร้าง query ใหม่ (New query)
2. เปิดไฟล์ `supabase/auth-schema.sql` ในโปรเจกต์ของคุณ
3. คัดลอกโค้ดทั้งหมด
4. วางใน SQL Editor
5. คลิก **Run**
6. ตรวจสอบว่า Table `profiles` ถูกสร้างแล้ว

---

### ขั้นตอนที่ 2: สร้าง Storage Bucket (3 นาที)

1. ใน Supabase Dashboard ไปที่ **Storage** (เมนูซ้าย)
2. คลิก **New bucket**
3. ตั้งค่า:
   - **Name**: `listings`
   - **Public bucket**: ✅ **เปิด** (สำคัญมาก!)
4. คลิก **Create bucket**

---

### ขั้นตอนที่ 3: ตั้งค่า Storage Policy (2 นาที)

1. ไปที่ **Storage** → **Policies**
2. คลิกที่ bucket `listings`
3. คลิก **New Policy**
4. เลือก **For full customization**
5. ตั้งชื่อ: `Allow public read access`
6. ใช้ SQL นี้:

```sql
CREATE POLICY "Allow public read access"
ON storage.objects FOR SELECT
USING (bucket_id = 'listings');
```

7. คลิก **Review** แล้ว **Save policy**

---

### ขั้นตอนที่ 4: รีสตาร์ทเซิร์ฟเวอร์

```bash
# หยุดเซิร์ฟเวอร์ (กด Ctrl+C ใน terminal)
# แล้วรันใหม่:
npm run dev
```

---

## ✅ ตรวจสอบการทำงาน

1. เปิดเบราว์เซอร์: **http://localhost:3000**
2. ไปที่ **http://localhost:3000/setup** - ควรเห็น ✅ ทั้งหมด
3. ทดสอบ:
   - สมัครสมาชิก
   - ลงประกาศ
   - อัปโหลดรูปภาพ
   - ค้นหา

---

## 🎉 เสร็จสิ้น!

ตอนนี้ Frontend และ Backend ทำงานร่วมกันแล้ว!

