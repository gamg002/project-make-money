# 🚀 Quick Start Guide

## ตั้งค่าโปรเจกต์ให้พร้อมใช้งานใน 5 นาที

### ขั้นตอนที่ 1: ตั้งค่า Supabase (2 นาที)

#### 1.1 สร้างบัญชีและโปรเจกต์
1. ไปที่ [supabase.com](https://supabase.com) → สร้างบัญชี
2. คลิก **New Project**
3. ตั้งชื่อโปรเจกต์ → ตั้งรหัสผ่าน Database → เลือก Region: **Southeast Asia**
4. รอ 2-3 นาทีให้สร้างเสร็จ

#### 1.2 หา API Keys
1. ไปที่ **Settings** → **API**
2. คัดลอก:
   - **Project URL** (ด้านบน)
   - **anon public** key (ในส่วน API keys)

#### 1.3 สร้างไฟล์ .env.local
ในโฟลเดอร์โปรเจกต์ สร้างไฟล์ `.env.local`:

```bash
# วิธีที่ 1: ใช้ script (แนะนำ)
./setup-env.sh

# วิธีที่ 2: สร้างเอง
touch .env.local
```

แล้วใส่ข้อมูล:
```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
```

---

### ขั้นตอนที่ 2: ตั้งค่า Database (2 นาที)

#### 2.1 รัน SQL Schema
1. ใน Supabase Dashboard → **SQL Editor**
2. คลิก **New query**
3. เปิดไฟล์ `supabase/schema.sql` → คัดลอกทั้งหมด → วาง → **Run**
4. เปิดไฟล์ `supabase/auth-schema.sql` → คัดลอกทั้งหมด → วาง → **Run**

#### 2.2 สร้าง Storage Bucket
1. ไปที่ **Storage**
2. คลิก **New bucket**
3. ตั้งชื่อ: `listings`
4. ✅ เลือก **Public bucket**
5. คลิก **Create bucket**

#### 2.3 ตั้งค่า Storage Policy
1. ไปที่ **Storage** → **Policies**
2. คลิกที่ bucket `listings`
3. คลิก **New Policy** → **For full customization**
4. ตั้งชื่อ: `Allow public read access`
5. ใช้ SQL:
```sql
CREATE POLICY "Allow public read access"
ON storage.objects FOR SELECT
USING (bucket_id = 'listings');
```
6. **Save policy**

---

### ขั้นตอนที่ 3: รีสตาร์ทเซิร์ฟเวอร์ (1 นาที)

```bash
# หยุดเซิร์ฟเวอร์ (กด Ctrl+C)
# แล้วรันใหม่:
npm run dev
```

เปิดเบราว์เซอร์: **http://localhost:3000**

---

## ✅ Checklist

- [ ] สร้าง Supabase project
- [ ] ตั้งค่า .env.local
- [ ] รัน schema.sql
- [ ] รัน auth-schema.sql
- [ ] สร้าง Storage bucket 'listings'
- [ ] ตั้งค่า Storage policy
- [ ] รีสตาร์ทเซิร์ฟเวอร์
- [ ] ทดสอบเปิดเว็บไซต์

---

## 🎉 พร้อมใช้งาน!

ตอนนี้คุณสามารถ:
- ✅ เปิดเว็บไซต์ได้
- ✅ สมัครสมาชิก
- ✅ ลงประกาศ
- ✅ ค้นหาประกาศ

---

## 🆘 มีปัญหา?

ดูใน:
- `SETUP.md` - คู่มือตั้งค่าแบบละเอียด
- `AUTH_GUIDE.md` - คู่มือระบบสมาชิก
- `README.md` - เอกสารหลัก

